{
    "name": "gaussian-splat-root",
    "private": true,
    "scripts": {
        "predev": "lsof -ti:8000 | xargs kill -9 || true",
        "dev": "echo 'Starting development server... This might take some time.' && concurrently \"npm run dev:backend\" \"npm run dev:frontend\" --kill-others --names \"BACKEND,FRONTEND\" --prefix-colors \"blue,green\"",
        "dev:frontend": "wait-on tcp:8000 && npm run dev --prefix app/frontend",
        "dev:backend": ".venv/bin/python app/backend/download_models.py && .venv/bin/python app/backend/main.py",
        "build": "npm run build --prefix app/frontend",
        "lint": "npm run lint --prefix app/frontend"
    },
    "devDependencies": {
        "concurrently": "^9.2.1",
        "wait-on": "^9.0.3"
    }
}